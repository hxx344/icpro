# ============================================================
# Streamlit 配置 (平台无关)
# ============================================================
# 服务器参数 (headless / CORS / address) 由 dashboard.py
# 根据当前操作系统自动设置，无需手动修改。
# ============================================================

[browser]
gatherUsageStats = false

[theme]
base = "dark"
