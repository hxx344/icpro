# Copy this file to /opt/project_Options/deploy/monitor-dashboard.env
# and replace placeholders before starting the monitor dashboard.
# The bundled systemd service listens on port 8502 by default.

MONITOR_USER=monitor
MONITOR_PASS=change_me_monitor
