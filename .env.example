# Binance European Options API credentials
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Dashboard login (leave empty to disable auth)
DASHBOARD_USER=
DASHBOARD_PASS=

# Optional: runtime mode
# Set to true for paper/simulated private endpoints when supported by config
# SIMULATE_PRIVATE=false
